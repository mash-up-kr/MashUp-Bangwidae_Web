[build]
  command = "yarn build"
  publish = ".next"

[context.production.environment]
  TOML_ENV_VAR = "From netlify.toml"
  NEXT_PUBLIC_TOML_ENV_VAR = "From netlify.toml (NEXT_PUBLIC_)"

[[redirects]]
  from = "/api/:path*"
  to = "https://doridori.ga/api/v1/:path*"
  status = 200
  force = true
