const QUERY_KEYS = {
  POST: 'questionDetail/post',
  COMMENT: 'questionDetail/comment',
  LIKE: 'questionDetail/like',
  UNLIKE: 'questionDetail/unlike',
};

export const { POST, COMMENT, LIKE, UNLIKE } = QUERY_KEYS;
