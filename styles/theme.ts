import { basic, error, gray, primary, secondary01, secondary02 } from './color';

const theme = {
  color: {
    basic,
    error,
    gray,
    primary,
    secondary01,
    secondary02,
  },
};

export default theme;
